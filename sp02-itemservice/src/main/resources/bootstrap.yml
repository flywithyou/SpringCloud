spring:
  cloud:
    inetutils:
      ignored-interfaces: VM.*
      preferred-networks: 176.117.81.214
    config:
      discovery:
        enabled: true
        service-id: config-server
      name: item-service
      profile: dev